apiVersion: v1
kind: Service
metadata:
  name: {{ template "kub.fullname" . }}
  labels:
    app: {{ template "kub.name" . }}
    chart: {{ template "kub.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "kub.name" . }}
    release: {{ .Release.Name }}
